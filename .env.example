# NWF Partnership Engine - Phase 1A
#
# LOCAL DEVELOPMENT ONLY. These are deterministic local Docker credentials.
# They are NOT secrets, NOT production values, and are NOT reused from any other
# system. This project has no managed database and no production environment.
#
# Copy to .env before running. .env is gitignored.

# --- Working database (real ingests live here) -------------------------------

# Owner/admin connection. Used ONLY by migrations (npm run db:migrate).
DATABASE_URL_ADMIN=postgres://nwf_owner:local_dev_only@127.0.0.1:55432/nwf_pe

# Ingest role. Least privilege: may INSERT/UPDATE organisations, INSERT
# organisation_sources (never UPDATE/DELETE), and INSERT/UPDATE ingest_runs.
DATABASE_URL_INGEST=postgres://nwf_ingest:local_dev_only@127.0.0.1:55432/nwf_pe

# Read-only role. Used by all `orgs`/`ingest runs` inspection commands.
DATABASE_URL_READONLY=postgres://nwf_readonly:local_dev_only@127.0.0.1:55432/nwf_pe

# Phase 2B research role (migration 0007). Least privilege, and stricter than
# nwf_ingest: SELECT on website_claims and organisations to obtain research
# roots, SELECT+INSERT on the orgunit_* evidence tables, and nothing else. No
# UPDATE, no DELETE, no TRUNCATE, no TEMPORARY, and no access at all to
# organisation_sources, ingest_runs or any ewp_ table.
DATABASE_URL_RESEARCH=postgres://nwf_research:local_dev_only@127.0.0.1:55432/nwf_pe

# Phase 2B-2 semantic-classifier role (migration 0009). Narrower than
# nwf_research: SELECT on organisations and the four Phase 2B-1 evidence
# tables (research runs, fetch observations, page evidence, page candidates),
# SELECT+INSERT on the four classifier tables, and nothing else. No UPDATE,
# no DELETE, no TRUNCATE, no TEMPORARY, and no access at all to
# website_claims or root authority.
DATABASE_URL_CLASSIFIER=postgres://nwf_classifier:local_dev_only@127.0.0.1:55432/nwf_pe

# --- Test database (integration tests TRUNCATE this one) ---------------------
#
# Deliberately a SEPARATE database. Integration tests truncate tables, so
# pointing them at the working database above would destroy ingested data on
# every `npm run validate`. The test helper refuses any database whose name does
# not end in `_test`.

DATABASE_URL_ADMIN_TEST=postgres://nwf_owner:local_dev_only@127.0.0.1:55432/nwf_pe_test
DATABASE_URL_INGEST_TEST=postgres://nwf_ingest:local_dev_only@127.0.0.1:55432/nwf_pe_test
DATABASE_URL_READONLY_TEST=postgres://nwf_readonly:local_dev_only@127.0.0.1:55432/nwf_pe_test
DATABASE_URL_RESEARCH_TEST=postgres://nwf_research:local_dev_only@127.0.0.1:55432/nwf_pe_test
DATABASE_URL_CLASSIFIER_TEST=postgres://nwf_classifier:local_dev_only@127.0.0.1:55432/nwf_pe_test

# --- Classifier runtime authentication (Phase 2B-2C2) ------------------------
#
# The semantic-classifier runtime authenticates EXCLUSIVELY with the owner's
# Claude subscription OAuth token, minted manually with `claude setup-token`
# and set here (or in the shell) as CLAUDE_CODE_OAUTH_TOKEN. It is required
# ONLY at the classifier pre-flight boundary; every other command and every
# automated test runs without it. NEVER commit a real token anywhere.
#
# ANTHROPIC_API_KEY must NOT be configured in this environment: the classifier
# pre-flight REFUSES to run (fail closed, no model call) when it - or any
# other conflicting auth/provider-routing variable such as
# ANTHROPIC_AUTH_TOKEN or a CLAUDE_CODE_USE_* cloud-provider switch - is
# present. There is no API/PAYG fallback path in this repository.
#
# CLAUDE_CODE_OAUTH_TOKEN=

# Optional. Set to 1 for verbose ingest diagnostics on stderr.
NWF_PE_VERBOSE=0
