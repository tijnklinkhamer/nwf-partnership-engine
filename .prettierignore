dist/
coverage/
node_modules/
package-lock.json
src/test/fixtures/

# Gitignored working directory for downloaded source artifacts.
data/

# Frozen empirical audit records are byte-frozen once landed: reformatting one
# would alter a historical record, so the formatter never touches them. (The
# 2026-08 shadow-validation audit predates the current prettier's markdown
# rules; this exemption preserves its exact committed bytes.)
docs/audits/

# Deterministically DERIVED evaluation evidence, excluded ONE EXACT FILE AT A
# TIME. A whole-directory rule here was tried and withdrawn (F4 -> F4A): it made
# every future file under docs/evaluation/results/ invisible to the formatter,
# which is exactly the blind spot 2D2B-1 acceptance closed. Only these named
# .jsonl files are exempt, and only because Prettier HAS NO .jsonl PARSER at all
# ("No parser could be inferred") -- one JSON object per line IS the format, and
# each line's bytes are pinned by a manifest SHA-256. The .json files beside them
# are deliberately NOT listed: the emitter lays them out with this repository's
# own Prettier, so `prettier --check` covers them like any other committed JSON.
docs/evaluation/results/phase2b-2d2c-dev-attribution-attempt-1/scored-items.jsonl
docs/evaluation/results/phase2b-2d2c-dev-attribution-attempt-1-gold-v1/scored-items.jsonl
docs/evaluation/results/phase2b-2d2c-dev-attribution-attempt-1-gold-v1-adjudicated/scored-items.jsonl
